# Dependencies
node_modules

# Build output
dist
dist-single
motomatch.html
*.tsbuildinfo

# Full-resolution photo originals (200+ MB). The site only needs the optimized
# WebP files in public/images/bikes/, which are committed. Regenerate them from
# originals with: node scripts/optimize-images.mjs
images-src/

# IDE / OS
.idea/
.vscode/
.DS_Store
*.local

# Local design references (not part of the app)
ChatGPT Image*.png
MotoMatch_Homepage_*.md
